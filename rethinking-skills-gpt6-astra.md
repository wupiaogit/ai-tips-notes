# 重新思考 GPT-6 Astra 下的 Skills 与提示词

> 原文：[Rethinking skills and prompts for GPT-6 Astra](https://x.com/pvncher/status/2095991462416490862)  
> 作者：eric provencher（[@pvncher](https://x.com/pvncher)）  
> 中文整理日期：2026-09-07

## 核心观点

编程 Agent 进步很快，过去一年为「扶着模型走」积累的大量指令，在 GPT-6 Astra 上往往已经过时，甚至变成**负优化**（占用上下文、拖慢工作、互相打架）。换新模型时，值得做一次「大扫除」。

这些指令主要来自三类：**Skills**、**AGENTS.md**、以及日常任务提示词。

---

## Skills：少而精，别堆满

Skill 本质是存成 Markdown（有时带脚本）的提示。适合「只在特定任务才需要」的流程，或某个插件的用法说明。

常见误区：往项目里塞一堆下载来的 Skills。

- 每个 Skill 的**名称 + 描述**都会进上下文，模型靠它们决定何时调用。
- 描述过长、Skill 过多时，Codex 会**压缩描述**，模型反而更难选对。
- 描述之间可能互相矛盾，或都在「抢戏」，导致加载了无用指令。

### 写好 Skill 的三条建议

1. **描述尽量短**，但要说清「什么时候该用」。  
   反例：写得像「凡是碰到数据库就用我」——模型会在不该用迁移相关 Skill 时也去调它。

2. **渐进披露（progressive disclosure）**  
   读一整份 Skill 会吃上下文、逼近 compaction，还可能塞进与当前任务无关的指导。多工作流时：根文档做成**最小路由**，指向子文档和脚本；让模型知道去哪找，而不是一次全读完。

3. **别写成过细的菜谱**  
   模型对模糊与 nuance 的理解变强了，过度具体的 itinerary 式指导，现在更容易帮倒忙。  
   仓库级 Skills 还会影响其他贡献者的 Agent（可能用不同模型）。对 Sol / Luna 有用的硬约束，可能把 Astra **拴得太死**。

（文中提到：若让 Codex 自己建 Skill，它常用 `$skill-creator`；官方已更新该 Skill，以减轻上述失败模式。）

---

## AGENTS.md：每条指令都问一句「还需要吗？」

`AGENTS.md` 会在仓库里**始终生效**，更要精简。

- 每次小改都先强制读一堆文档 / 全仓地图：对改个 typo 太重。Astra 自己会判断该读什么。
- 「每次编辑前先读文件」很容易**烧上下文、拖慢节奏**。指向文档可以，但要**有上下文**，且文档本身要保持更新。
- 旧模型需要催着跑测试、检查结果；Astra **默认就会做**，同样指令可能变成多余测试。
- Astra 很细心，但有时对「任务该做到哪一步」偏谨慎。可用 `AGENTS.md` 明确授权某个你确认安全的流程，例如本地测试套件可反复跑、修失败、再跑，而不必每步请示。

---

## 决策边界：别用旧恐惧绑住新模型

如果以前模型会「没问就替你干」，你可能加了很多「必须先问再做」的强硬措辞。  
Astra 判断力更好，也更当真——边界写得太死时，它可能在你其实希望继续的地方停住。

---

## 持续性：先定义「完成」再开工

习惯 Sol 那种一接到请求就长跑的人，会觉得 Astra 更「试探」：做出第一版实现就回来等你看，后面其实还有活。

建议：

- 开工前写清完成标准（要实现、跑起来、检查结果、修失败等）。
- 「第一版实现后必须停下来等 review」会把模型往更早的停点拉——确认你是否真需要这一步。
- 若希望它探索得更远，说清探索什么、在哪里停。

---

## 行动建议

新模型是整理「指令负债」的好时机：按上文原则让 Astra **审计现有 Skills 与 AGENTS.md**，清掉负优化，再去做以前不敢碰的事。

### 可直接丢给 Codex 的审计提示（来自社区跟帖用法）

```text
请阅读文章 https://x.com/pvncher/status/2095991462416490862 ，然后重新审计 ~/Projects 内的所有技能和 AGENTS.md 文件
```

（路径按你本机项目目录改。）
